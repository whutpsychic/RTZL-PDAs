<?xml version="1.0" encoding="utf-8"?>
<resources xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- 基主题继承自Theme.AppCompat -->
    <style name="Base.Theme.RunnableDemo" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- 自定义主题属性 -->
    </style>

    <!-- 应用主题 -->
    <style name="Theme.RunnableDemo" parent="Base.Theme.RunnableDemo" />

    <style name="Theme.RtlinkAndroidApp" parent="android:Theme.Material.Light.NoActionBar">
        <!--背景填充色-->
        <item name="android:colorBackground">@color/white</item>
        <!--背景图-->
<!--        <item name="android:windowBackground" android:tilleMode="clamp">@mipmap/launch_image</item>-->
    </style>

    <style name="Theme.default" parent="android:Theme.Material.Light.NoActionBar">
        <!--背景填充色-->
        <item name="android:colorBackground">@color/white</item>
        <!--背景图-->
        <item name="android:windowBackground">@color/white</item>
    </style>

</resources>